definitions:
  Message:
    type: 'object'
    properties:
      id:
        type: 'integer'
        description: 'Unique id'
      value:
        type: 'string'
      type:
        type: 'string'
        enum: ['text', 'file']
      memberId:
        type: 'integer'
      channelId:
        type: 'integer'
      createdAt:
        type: 'string'
        format: 'date-time'
      updatedAt:
        type: 'string'
        format: 'date-time'
